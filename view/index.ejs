<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
    <style>
      body {
        background: linear-gradient(135deg, #e0f7fa, #e3f2fd);
        min-height: 100vh;
      }

      .card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      }

      .btn {
        transition: background-color 0.3s ease;
      }

      .btn-primary:hover {
        background-color: #0d6efdcc;
      }

      .btn-success:hover {
        background-color: #198754cc;
      }

      h2 {
        font-weight: 700;
        color: #2c3e50;
      }

      .card-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: #34495e;
      }

      .card-text {
        color: #666;
      }
    </style>
  </head>
  <body>
    <div class="container py-5">
      <h2 class="mb-5 text-center">Selamat Datang di Dashboard Admin</h2>

      <div class="row justify-content-center g-4">
        <!-- Card: Sistem Admin Page -->
        <div class="col-md-4">
          <div class="card shadow-sm border-0">
            <div class="card-body text-center">
              <div class="mb-3">
                <i class="bi bi-speedometer2 fs-1 text-primary"></i>
              </div>
              <h5 class="card-title">Sistem Admin</h5>
              <p class="card-text">Kelola data produk, pembelian, stok, dan lainnya di panel admin.</p>
              <a href="/produk" class="btn btn-primary mt-2"><i class="bi bi-box-arrow-in-right me-1"></i>Masuk Sistem</a>
            </div>
          </div>
        </div>

        <!-- Card: ChatBot -->
        <div class="col-md-4">
          <div class="card shadow-sm border-0">
            <div class="card-body text-center">
              <div class="mb-3">
                <i class="bi bi-robot fs-1 text-success"></i>
              </div>
              <h5 class="card-title">ChatBot Admin</h5>
              <p class="card-text">Dapatkan bantuan cepat dari sistem chatbot pintar untuk admin toko.</p>
              <a href="/chatbot" class="btn btn-success mt-2"><i class="bi bi-chat-dots me-1"></i>Buka ChatBot</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
